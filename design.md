# Dify 作品矩阵设计 v1 — 对齐小红书 AI 策略产品岗位画像

> 思路：从岗位需要的 4 层能力（LLM 深度理解 / 评估方法论 / 信源链路 / 反馈闭环）倒推 4 个作品，每个作品对应一个能力层，面试时逐个对答「这个作品证明我有你们要的 X 能力」。

## 总览

| # | 作品 | 类型 | 对应能力层 | 对应 JD 职责 | 差异化 |
|---|---|---|---|---|---|
| ① | 模型能力与算力评估 Agent | Chatflow | LLM 深度理解 | 隐性要求（懂模型） | 竞争者无人做；蹭 2026 模型选型热度；用户本地部署经历直接复用 |
| ② | 外部信源验收 Agent | Chatflow/Workflow | 信源链路 | 职责二（外部信源接入） | 简历此块只有间接证据 → 直接证据 |
| ③ | 回答质量评估 Agent | Chatflow | 评估方法论 | 职责一（评估体系） | 评测项目产品化，rubric 复用 |
| ④ | 用户反馈归因 Agent | Workflow | 反馈闭环 | 职责三（反馈闭环） | 反馈→case 沉淀流程化 |

共享资产：4 维 rubric（rubric.md）、badcase 归因模板、模型知识库。

---

## ① 模型能力与算力评估 Agent（Chatflow）

**定位**：帮团队做模型选型的「模型顾问」——输入场景/预算/硬件，输出推荐 + 架构说明 + 算力需求 + 能力边界。

**输入**（用户自由描述，Agent 可追问）：
- 任务场景（中文办公/客服/代码/多模态/Agent 编排）
- 预算与部署环境（API / 本地 GPU 配置 / 云）

**知识资产**（提示词内置或知识库）：
- Qwen3.6 (35B)：架构（MHA/GQA？）、显存需求（FP16/INT4/INT8）、能力特征、部署经验
- Qwen3.8 (27B)：同上
- DeepSeek / Kimi / GLM 等 API 模型：能力边界、成本
- 通用规则：显存估算公式（参数量 × 精度字节 × 1.2 开销）、量化方式对比、推理 vs 训练资源

**输出**：结构化推荐报告
```
场景：{输入}
推荐：{模型}（{理由}）
架构要点：{参数量/注意力类型/上下文}
算力需求：{显存估算（FP16/INT4）+ 推理吞吐参考}
能力边界：{擅长/不擅长 + 已知坑}
备选：{备选模型及适用条件}
```

**真实经历支撑**：用户本地部署过 Qwen3.6/3.8，可回答任何追问（量化、上下文长度、多卡）。

---

## ② 外部信源验收 Agent（Chatflow）

**定位**：对齐 JD「外部信源与合作方接入策略——需求定义、字段/内容验收、上线验证、效果评估」。

**输入**：
- 信源描述（URL / 字段样例 / 内容类型 / 更新频率）
- 计划用途（回答链路中哪个环节：事实补充 / 时效更新 / 引用）

**验收流程**（Agent 按清单逐项检查）：
1. 字段完整性：来源可溯 / 时效标注 / 内容去重 / 格式规范（对照 checklist）
2. 内容质量抽检：抽取样本 → 准确性 / 可信度 / 相关性评估
3. 上线风险：隐私合规 / 版权 / 内容冲突
4. 验收结论：✅ 通过 / ⚠️ 有条件通过（列出条件）/ ❌ 拒绝（列出原因）

**输出**：验收报告（JSON 结构化），含「验收清单 + 每项结论 + 证据 + 风险点 + 上线验证建议」。

**故事**：知识图谱 OCR 管线（字段定义/质量校验/上线验证）的方法论，产品化为可复用的验收工具。

---

## ③ 回答质量评估 Agent（Chatflow）

**定位**：评测项目的产品化——输入「问题+回答」，输出 4 维评分 + badcase 归因。

**输入**：
- 用户问题（必填）
- 模型回答（必填）
- 参考标准答案/领域背景（选填，可加分）

**处理**：
1. 按 rubric 4 维打分（准确性/相关性/可信度/完整性，1-5）
2. 每个 ≤3 分维度强制摘录原文证据
3. badcase 归因（维度/严重度/归因类型/优化方向）

**输出**：结构化评分报告 + 优化建议。

**故事**：28 条用例评测项目的 rubric 直接复用——「我不只做过一次评估，我把它做成了工具」。

---

## ④ 用户反馈归因 Agent（Workflow）

**定位**：对齐 JD「建立用户反馈到产品迭代的闭环机制，收集并分析真实用户反馈，将问题沉淀为评估 case、BadCase 归因和优化方向」。

**输入**：用户反馈文本（支持多条批量，逗号/换行分隔）

**处理流程**（Workflow 节点）：
1. 预处理：拆分/去重
2. 聚类：按问题类型分组（准确性 / 时效 / 理解错误 / 指令跟随 / 体验问题）
3. 严重度评估：高（误导用户）/ 中 / 低
4. 归因：知识截止 / 歧义未消解 / 覆盖不全 / 幻觉 / 信源缺失
5. 沉淀：输出「新评估 case 建议清单」（可直接进入用例集）

**输出**：反馈分析报告（聚类表 + 严重度排序 + case 建议清单）。

**故事**：「反馈不是投诉，是可复用的测试集——我把闭环流程化了」。

---

## 实施顺序（按差异化强度 × 素材就绪度）

1. ③ 回答评估 Agent（rubric 现成，最快，验证 Dify 流程）→ 或先 ①（用户新经历，最有感觉）
2. ① 模型评估 Agent（需要整理模型库知识）
3. ② 信源验收 Agent（checklist 设计）
4. ④ 反馈归因 Agent（Workflow 批量）

每个作品：搭 → 测 3 组输入 → 截图 → 记录到本目录作品说明。

## 共享资产

- rubric.md（评测项目）→ ③ 用
- badcase 归因模板（评测项目 report.md）→ ③④ 用
- 模型知识库（新建 model_kb.md）→ ① 用
- 信源验收 checklist（新建 source_checklist.md）→ ② 用
